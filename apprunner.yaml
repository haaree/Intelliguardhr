version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "Using Docker build - this file is for reference only"
run:
  command: npm start
  network:
    port: 3006
  env:
    - name: NODE_ENV
      value: production
