version: 0.2

phases:
  pre_build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...
  build:
    commands:
      - docker build -t intelliguard-hr:latest .
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
